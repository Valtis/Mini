<h1>Images</h1>


<div class="row">

  <% @images.each do |image| %>
      <div class="col-lg-3 col-sm-4 col-xs-6">
        <%= link_to "#{image_tag(image.S3Image.url(:medium), class: 'img-responsive')}".html_safe, image, class: 'thumbnail pull-left' %>
      </div>
  <% end %>
</div>